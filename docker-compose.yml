services:
  postgres:
    image: postgres:17
    container_name: acres-postgres
    environment:
      POSTGRES_DB: acres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - acres_pgdata:/var/lib/postgresql/data
volumes:
  acres_pgdata:
