package com.example.acres.dto; import java.time.*; public class AdminDtos { public record UserResponse(Long id,String name,String email,String role,boolean active,boolean mustChangePassword,Instant createdAt,Instant updatedAt,Instant lastLoginAt){} public record AuditResponse(Long id,Long userId,String userName,String action,String entityType,String entityId,String oldValue,String newValue,Instant createdAt,String ipAddress){} }
